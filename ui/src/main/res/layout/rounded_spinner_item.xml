<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/tv_item_selected"
        android:layout_width="match_parent"
        android:layout_height="58dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:ellipsize="marquee"
        android:gravity="center_vertical"
        android:singleLine="true"
        android:textColor="@color/line_gray"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Selecione o tipo de consulta"/>

    <ImageView
        android:id="@+id/arrow_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:gravity="center_vertical|end"
        app:layout_constraintTop_toTopOf="@+id/tv_item_selected"
        app:layout_constraintBottom_toBottomOf="@+id/tv_item_selected"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/ic_arrow_spinner_black"
        android:contentDescription="@string/arrow" />

    <View
        android:id="@+id/dividerItemRecord"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginHorizontal="1dp"
        android:background="@color/line_gray"
        app:layout_constraintTop_toBottomOf="@+id/tv_item_selected"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>